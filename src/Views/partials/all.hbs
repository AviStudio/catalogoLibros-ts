<div class="row">
  <div class="col-sm-8">

    {{#each Libros}}
      <div class="card d-flex flex-row flex-wrap">
        <img src="{{foto_link}}" alt="" class="w-25 h-100 my-auto m-3" />
        <div class="card-body">
          <ul class="list-group">
            <li class="list-group-item">{{titulo}}</li>
            <li class="list-group-item">{{autor}}</li>
            <li class="list-group-item">{{edicion}}° Edición</li>
            <li class="list-group-item">{{editorial}}, {{pais}}, {{anio}}</li>
            <li class="list-group-item">Genero:
              {{Categorium.nombre_categoria}}</li>
          </ul>
          <div class="btn-group mt-2">
            <form action="/cms/delete/{{id}}?_method=delete" method="POST">
              <input type="hidden" name="_method" value="delete" />
              <button type="submit" class="btn btn-danger">Delete</button>
            </form>
            <form action="/cms/edit/{{id}}" method="get">
              <button type="submit" class="btn btn-primary">Edit</button>
            </form>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>